// let's define a conditional statements using JS

let fruit = prompt('What is your favorite fruit?', 'Banana');
if (fruit === 'Banana') {
	console.log(fruit);
	console.log('The if block is executed since the if statement is true');
}
else {
	console.log('The else block is executed since the if statement is false');
}